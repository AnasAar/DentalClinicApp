{% extends 'base.html.twig' %}

{% block title %}Facture{% endblock %}

{% block body %}
<h1><span class="fa fa-stethoscope" aria-hidden="true"></span>Facture</h1>


    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ facture.id }}</td>
            </tr>
            <tr>
                <th>Tarif</th>
                <td>{{ facture.tarif }}</td>
            </tr>
            <tr>
                <th>Date_facture</th>
                <td>{{ facture.dateFacture ? facture.dateFacture|date('Y-m-d') : '' }}</td>
            </tr>
            <td>
                <div class="login-icon ml-2">
                    <a class="" href="{{ path('facture_index') }}"> back to list </a>

                    <a class="" href="{{ path('facture_edit', {'id': facture.id}) }}"> edit </a>
                </div>
            </td>
        </tbody>
    </table>

    {{ include('facture/_delete_form.html.twig') }}
{% endblock %}
