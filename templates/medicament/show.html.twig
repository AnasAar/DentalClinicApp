{% extends 'base.html.twig' %}

{% block title %}Medicament{% endblock %}

{% block body %}
<h1><span class="fa fa-stethoscope" aria-hidden="true"></span>Medicament</h1>


    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medicament.id }}</td>
            </tr>
            <tr>
                <th>Nom_medi</th>
                <td>{{ medicament.nomMedi }}</td>
            </tr>
            <tr>
                <th>Dose_medi</th>
                <td>{{ medicament.doseMedi }}</td>
            </tr>
            <td>
                <div class="login-icon ml-2">
                    <a class="" href="{{ path('medicament_index') }}"> back to list </a>

                    <a class="" href="{{ path('medicament_edit', {'id': medicament.id}) }}"> edit </a>
                </div>
            </td>
        </tbody>
    </table>

    {{ include('medicament/_delete_form.html.twig') }}
{% endblock %}
