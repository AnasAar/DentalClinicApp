{% extends 'base.html.twig' %}

{% block title %}Rendez-vous index{% endblock %}

{% block body %}
<h1><span class="fa fa-stethoscope" aria-hidden="true"></span>Rendez-vous index</h1>


    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date_rdv</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for rdv in rdvs %}
            <tr>
                <td>{{ rdv.id }}</td>
                <td>{{ rdv.dateRdv ? rdv.dateRdv|date('Y-m-d H:i:s') : '' }}</td>
                <td><div class="login-icon ml-2">
                    <a class="" href="{{ path('rdv_show', {'id': rdv.id}) }}"> show </a>
                    <a class="" href="{{ path('rdv_edit', {'id': rdv.id}) }}"> edit </a>
                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
<div class="login-icon ml-2">
<a class="" href="{{ path('rdv_new') }}"> Create new </a>
</div>

<div class="login-icon ml-2">
<a class="" href="{{ path('admin') }}"> Page admin </a>
</div>
{% endblock %}
