{% extends 'base.html.twig' %}

{% block title %}Rendez-vous{% endblock %}

{% block body %}
<h1><span class="fa fa-stethoscope" aria-hidden="true"></span>Rendez-vous</h1>


    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rdv.id }}</td>
            </tr>
            <tr>
                <th>Date_rdv</th>
                <td>{{ rdv.dateRdv ? rdv.dateRdv|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Patient</th>
                <td>{{ rdv.patient }}</td>
            </tr>
            <tr>
                <th>Medcin</th>
                <td>{{ rdv.medcin }}</td>
            </tr>
            <td>
                <div class="login-icon ml-2">
                    <a class="" href="{{ path('rdv_index') }}"> back to list </a>

                    <a class="" href="{{ path('rdv_edit', {'id': rdv.id}) }}"> edit </a>
                </div>
            </td>
        </tbody>
    </table>

    {{ include('rdv/_delete_form.html.twig') }}
{% endblock %}
