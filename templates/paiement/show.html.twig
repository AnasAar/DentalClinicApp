{% extends 'base.html.twig' %}

{% block title %}Paiement{% endblock %}

{% block body %}
<h1><span class="fa fa-stethoscope" aria-hidden="true"></span>Paiement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paiement.id }}</td>
            </tr>
            <tr>
                <th>Mode_paiment</th>
                <td>{{ paiement.modePaiment }}</td>
            </tr>
            <tr>
                <th>Montant_paye</th>
                <td>{{ paiement.montantPaye }}</td>
            </tr>
            <tr>
                <th>Montantnon_paye</th>
                <td>{{ paiement.montantnonPaye }}</td>
            </tr>
            <td>
                <div class="login-icon ml-2">
                    <a class="" href="{{ path('paiement_index') }}"> back to list </a>

                    <a class="" href="{{ path('paiement_edit', {'id': paiement.id}) }}"> edit </a>
                </div>
            </td>
        </tbody>
    </table>

    {{ include('paiement/_delete_form.html.twig') }}
{% endblock %}
