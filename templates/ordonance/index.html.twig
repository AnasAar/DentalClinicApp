{% extends 'base.html.twig' %}

{% block title %}Ordonance index{% endblock %}

{% block body %}
<h1><span class="fa fa-stethoscope" aria-hidden="true"></span>Ordonance index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date_ord</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ordonance in ordonances %}
            <tr>
                <td>{{ ordonance.id }}</td>
                <td>{{ ordonance.dateOrd ? ordonance.dateOrd|date('Y-m-d') : '' }}</td>
                <td><div class="login-icon ml-2">
                    <a class="" href="{{ path('ordonance_show', {'id': ordonance.id}) }}"> show </a>
                    <a class="" href="{{ path('ordonance_edit', {'id': ordonance.id}) }}"> edit </a>
                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
<div class="login-icon ml-2">
<a class="" href="{{ path('ordonance_new') }}"> Create new </a>
</div>

<div class="login-icon ml-2">
<a class="" href="{{ path('admin') }}"> Page admin </a>
</div>

{% endblock %}
