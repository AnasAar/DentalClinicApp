{% extends 'base.html.twig' %}

{% block title %}Edit Consultation{% endblock %}

{% block body %}
<h1><span class="fa fa-stethoscope" aria-hidden="true"></span>Edit Consultation</h1>

    {{ include('consultation/_form.html.twig', {'button_label': 'Update'}) }}

<div class="login-icon ml-2">
    <a class="" href="{{ path('consultation_index') }}"> back to list </a>
</div>

    {{ include('consultation/_delete_form.html.twig') }}
{% endblock %}
