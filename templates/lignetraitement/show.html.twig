{% extends 'base.html.twig' %}

{% block title %}Lignetraitement{% endblock %}

{% block body %}
<h1><span class="fa fa-stethoscope" aria-hidden="true"></span>Ligne traitement</h1>


    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lignetraitement.id }}</td>
            </tr>
            <tr>
                <th>Nom_dent</th>
                <td>{{ lignetraitement.nomDent }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ lignetraitement.dateDebut ? lignetraitement.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ lignetraitement.dateFin ? lignetraitement.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Type_traitement</th>
                <td>{{ lignetraitement.typeTraitement }}</td>
            </tr>
            <tr>
                <th>Fiche patient</th>
                <td>{{ lignetraitement.fiche }}</td>
            </tr>
            <td>
                <div class="login-icon ml-2">
                    <a class="" href="{{ path('lignetraitement_index') }}"> back to list </a>

                    <a class="" href="{{ path('lignetraitement_edit', {'id': lignetraitement.id}) }}"> edit </a>
                </div>
            </td>
        </tbody>
    </table>

    {{ include('lignetraitement/_delete_form.html.twig') }}
{% endblock %}
