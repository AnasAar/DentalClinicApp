{% extends 'base.html.twig' %}

{% block title %}Medicament index{% endblock %}

{% block body %}
<h1><span class="fa fa-stethoscope" aria-hidden="true"></span>Medicament index</h1>


    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom_medi</th>
                <th>Dose_medi</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for medicament in medicaments %}
            <tr>
                <td>{{ medicament.id }}</td>
                <td>{{ medicament.nomMedi }}</td>
                <td>{{ medicament.doseMedi }}</td>
                <td><div class="login-icon ml-2">
                    <a class="" href="{{ path('medicament_show', {'id': medicament.id}) }}"> show </a>
                    <a class="" href="{{ path('medicament_edit', {'id': medicament.id}) }}"> edit </a>
                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
<div class="login-icon ml-2">
<a class="" href="{{ path('medicament_new') }}"> Create new </a>
</div>

<div class="login-icon ml-2">
<a class="" href="{{ path('admin') }}"> Page admin </a>
</div>

{% endblock %}
